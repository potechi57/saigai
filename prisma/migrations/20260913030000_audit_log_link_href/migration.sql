-- 編集履歴（AuditLog）を、カルテ（点検調書＞災害）以外の登録・編集操作
-- （施設台帳のExcel取込・削除、台帳（画像）の登録・編集・削除・画像追加等）でも
-- 記録できるようにする。既存のkarteFacilityNoはカルテ専用のリンク手段のため
-- 変更せず、それ以外の対象向けに詳細ページのパスをそのまま保持するlinkHrefを
-- 追加する（会話ログ「それ以外の登録や編集の内容を表示するようにしてほしい」参照）。

ALTER TABLE "AuditLog" ADD COLUMN "linkHref" TEXT;
